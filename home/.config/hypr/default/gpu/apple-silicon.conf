#  Apple Silicon (Asahi Linux)
# Specific configuration for M-series GPUs

# Hardware cursors are generally supported now, but disabling them 
# is a safe fallback if you encounter cursor glitches.
cursor:no_hardware_cursors = true

# Use standard WLR renderer
# env = WLR_RENDERER,vulkan 
# ^ Vulkan is experimental on Asahi, OpenGL (default) is stable.
